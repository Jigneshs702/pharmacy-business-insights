---
interface Props {
  heading: string;
  text: string;
  href: string;
  button: string;
}
const { heading, text, href, button } = Astro.props;
---
<div class="cta-box">
  <h3>{heading}</h3>
  <p>{text}</p>
  <a href={href} class="btn">{button}</a>
</div>
